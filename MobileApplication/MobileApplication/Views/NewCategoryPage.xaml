<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MobileApplication.Views.NewCategoryPage"
             Shell.PresentationMode="ModalAnimated"
             Title="New Category">
    <ContentPage.Content>
        <StackLayout Spacing="3" Padding="15">
            <Label Text="Category" FontSize="Medium"/>
            <Entry Text="{Binding CategoryTitle, Mode=TwoWay}" FontSize="Medium"/>
            <Label Text="Dedicated Amount" FontSize="Medium"/>
            <Editor Text="{Binding DedicatedAmount, Mode=TwoWay}" AutoSize ="TextChanges"  FontSize="Medium" Margin="0"/>
            <StackLayout Orientation="Horizontal">
                <Button Text="Cancel" Command="{Binding CancelCommand}" HorizontalOptions="FillAndExpand"></Button>
                <Button Text="Save" Command="{Binding SaveCommand}" HorizontalOptions="FillAndExpand"></Button>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>